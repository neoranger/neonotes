# Configuración de NeoNotes
# Copia este archivo a `.env` y ajusta los valores.

# Secreto usado para firmar los tokens JWT.
# OBLIGATORIO en producción: genera uno con `openssl rand -base64 48`.
JWT_SECRET=